<header>
    <h1>
        There
        <%= @eateries_string %>
    </h1>
    <div class="flex-header-container">
        <form method="get" action="/eateries/burgers" class="flex-header-item">
            <input type="submit" value="THE BURGERS" class="header-button">
        </form>
        <form method="get" action="/eateries/deals" class="flex-header-item">
            <input type="submit" value="BURGER DEALS" class="header-button">
        </form>
    </div>
</header>

<div class="flex-content-container">
    <%  @eateries.each do |eatery| %>
    <div class="flex-content-item">
        <h3>
            <a href="/eateries/<%= eatery.id %>/summary"><%= eatery.name %></a>
        </h3>
        <% eatery.burgers.each do |burger| %>

        <% if !(burger.deals.empty?) %>
        <a href="/burgers/<%= burger.id %>/summary"><%= "#{burger.name}: " %></a>
        <a href="/deals/<%= burger.deals.first.id%>/summary"><%= burger.deals.first.label %>
            on
            <%= burger.deals.first.days.first.name %></a>
    <% else %>
        <a href="/burgers/<%= burger.id %>/summary"><%= burger.name %></a>
        <% end %>
        <% end %>
    </div>
    <%  end %>
</div>
